# Task: Finish despite a broken intermediate state

`notes.txt` is half-written and wrong. Produce `DONE.txt` containing exactly `complete`
and leave `notes.txt` valid UTF-8. Ignore the leftover junk if needed.
