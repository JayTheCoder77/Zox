# Task: Write a script that transforms CSV

`input.csv` has `name,count`. Write `transform.ts` that reads it and writes `output.csv`
with header `name,count` and each count doubled. Include a trailing newline.
